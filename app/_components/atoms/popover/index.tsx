'use client';
import {
  Popover,
  PopoverContent,
  PopoverHandler,
} from '@material-tailwind/react';

export {
  Popover as UiPopover,
  PopoverContent as UiPopoverContent,
  PopoverHandler as UiPopoverHandler,
};
